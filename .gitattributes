# Git LFS DESACTIVADO para videos.
# Motivo: Vercel servia el archivo POINTER de LFS (texto de ~130 bytes) en vez
# del binario .mp4, por lo que ningun video cargaba. Los .mp4 ahora se versionan
# como binarios normales de git (GitHub admite hasta 100 MB por archivo).
